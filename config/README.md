# Configuration

Bezpečné, verzované konfigurační šablony pro debug, staging a release. Secrets,
signing identity, provisioning profily, APNs klíče a produkční managed config se
do repozitáře neukládají.
